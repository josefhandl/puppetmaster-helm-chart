apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: puppetserver-np
spec:
  podSelector:
    matchLabels:
      app: puppetserver
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - ports: # expose this PuppetServer
    - protocol: TCP
      port: 8140
    from:
    - podSelector: # allow from PuppetDB
        matchLabels:
          app: puppetdb
  egress:
  - ports: # to DNS
    - protocol: UDP
      port: 53
    to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: kube-system
      podSelector:
        matchLabels:
          k8s-app: kube-dns
  - ports: # to CloudNativePG
    - port: 5432
      protocol: TCP
    to:
    - podSelector:
        matchLabels:
          cnpg.io/cluster: puppetdb-cnpg